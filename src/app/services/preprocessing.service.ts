import { Injectable } from '@angular/core';

@Injectable({
    providedIn: 'root'
})
export class PreprocessingService {

    public variableJson: any;
    
    constructor() { }

    public nameFunction(year: number, income: number): void {
        // TODO: Add code
    }
}
